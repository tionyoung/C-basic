/*
#include <stdio.h>
int get_sum(int array[], int size);
int main(void) {
	int total;
	int m[3][6] = { 10,10,10,10,10,10, 10,10,10, 10,10,10, 10,10,10, 10,10,10 };
	total=get_sum(m, sizeof(m) / 4);
	printf("정수의 합=%d", total);
	return 0;
}

int get_sum(int array[], int size) {
	int hap = 0;
	for (int i = 0; i < size; i++) {
		hap += array[i];
	}
	return hap;
}
*/