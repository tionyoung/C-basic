/*
#include <stdio.h>
int main(void) {

	int grade = 0;
	int sum = 0;
	double average = 0.0;
	int n = 0;

	while (grade >= 0)
	{
		printf("성적을 입력하시오:");
		scanf_s("%d", &grade);

		if (grade < 0)
			break;

		sum += grade;
		n++;
	}

	sum = sum - grade;

	average = sum / n;
	printf("성적의 평균은 %lf입니다.", average);

	return 0;
*/